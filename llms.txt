# tosijs-product v0.6.5

Scroll-linked animation components for tosijs.

- Docs: https://product.tosijs.net
- Source: https://github.com/tonioloewald/tosijs-product
- npm: https://www.npmjs.com/package/tosijs-product

## Documentation

Full documentation, with live code examples, is at the links below.

Live examples run inline on the shared page by default (not sandboxed):
each has its own `preview` element, but they share one tosijs module, so
`tosi()` state singletons are shared across every example on a page. This is
deliberate (it shows tosi isolation and lets you drive demos via the global
singleton). Namespace example state with unique top-level keys so examples
don't clobber each other; the `iframe` attribute isolates DOM/CSS, not state.

## Pages

- https://product.tosijs.net/tosi-filmstrip/ — <tosi-filmstrip>
  Canvas frame-animator. Rather than scrubbing a `<video>` (which stutters on random-access seeks),
- https://product.tosijs.net/tosi-interpolator/ — <tosi-interpolator>
  Declarative CSS-property interpolation between scroll **waypoints**. Wrap any element, add
- https://product.tosijs.net/tosi-prism/ — <tosi-prism>
  Lazy-loads PrismJS (from jsDelivr) to syntax-highlight its own text content. Set the language with
- https://product.tosijs.net/tosi-product/ — <tosi-product>
  The scroll engine. A `<tosi-product>` owns the scrollable region it lives in: it measures a
- https://product.tosijs.net/tosi-b3d-scroll/ — <tosi-scroll-camera> · <tosi-scroll-time> · <tosi-scroll-animation>
  Scroll controllers for a tosijs-ui [`<tosi-3d>`](https://ui.tosijs.net/babylon-3d) BabylonJS scene.
- https://product.tosijs.net/tosi-scroll-map/ — <tosi-scroll-map>
  Waypoint-driven scroll controller for a tosijs-ui [`<tosi-map>`](https://ui.tosijs.net/mapbox)
- https://product.tosijs.net/components/ — Component Reference
  Every element and exported function in tosijs-product: the scroll engine, the animators, the theme system, and the JavaScript API.
- https://product.tosijs.net/getting-started/ — Getting Started
  A single script tag gives you everything — tosijs, tosijs-ui, and tosijs-product with all custom elements registered:
- https://product.tosijs.net/ — tosijs-product
  Build Apple-style, scroll-linked product stories with declarative web components. tosijs-product pins sections and drives Lottie, video (WebP mosaics), BabylonJS 3D, and CSS interpolation from scroll position — authored in plain HTML.
